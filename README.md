# The Anatomy of Ideas

An interactive atlas of philosophy, psychology, and consciousness. **246 ideas** across
**eleven great questions**, cross-linked to a **425-star constellation** of the thinkers and
schools that shaped them. It is *idea-centric* rather than name-centric: the unit is the
concept, and people orbit the concepts they originated.

The whole thing is a single self-contained HTML file with no backend — `index.html`.
Drop it on any static host and it runs.

---

## Quick start

**Just deploy it:** `index.html` (at the repo root) is prebuilt and standalone — D3 and the
two fonts are inlined, so it makes **zero network calls** at runtime. Upload it anywhere
(Netlify, GitHub Pages, Cloudflare Pages, S3, Vercel). On GitHub Pages, serve from the repo
root and `index.html` is picked up automatically.

**Rebuild it from source:**

```bash
python3 build.py          # -> index.html (repo root)   (needs Python 3, no pip deps)
```

**Test it:**

```bash
npm install               # jsdom + d3, for the headless smoke test
npm test                  # exercises all 5 modes, filters, threads, deep-links, reading store
```

---

## What's in the box

```
anatomy-of-ideas/
├── index.html             ← the deployable. One file, no backend, zero network calls.
├── src/
│   ├── concepts.py         ← base data: QUESTIONS, concepts (C), links (R), dialectics (ARG)
│   ├── expand.py           ← round 1 additions (consciousness studies, Islamic/mystical, …)
│   ├── expand2.py          ← round 2 (Performance Trap, Withdrawal, Death-facing, latest voices)
│   ├── expand3.py          ← round 3 (the engagement counter-pole + women thinkers)
│   ├── expand4.py          ← round 4 (the Literature & Imagination canon + de Beauvoir)
│   ├── expand5.py          ← round 5 (African & Indigenous clean tier: Ubuntu, Àṣẹ, perspectivism…)
│   ├── expand6.py          ← round 6 (Korean clean tier: Four-Seven Debate, Hwajaeng, Innaecheon…)
│   ├── expand7.py          ← round 7 (scripture-born: Agape, Grace, Covenant, Imago Dei, Tawhid…)
│   ├── expand8.py          ← round 8 (other civilizations: Zoroastrian, Nahua, Gilgamesh, Ma'at…)
│   ├── expand9.py          ← round 9 (Hermetic & Esoteric + Norse: As Above So Below, Gnosis, Ragnarök…)
│   ├── expand10.py         ← round 10 (The Secret of the Golden Flower — Daoist inner alchemy)
│   ├── expand11.py         ← round 11 (practice texts: Hesychasm, Spiritual Exercises, Dzogchen, mindfulness, Dhikr)
│   ├── readings.py         ← ranked thinkers + readings for the base concepts
│   ├── readings_extra.py   ← readings for round-1 concepts
│   ├── readings_extra2.py  ← readings for round-2 concepts
│   ├── readings_extra3.py  ← readings for round-3 concepts
│   ├── readings_extra4.py  ← readings for round-4 concepts
│   ├── readings_extra5.py  ← readings for round-5 concepts
│   ├── readings_extra6.py  ← readings for round-6 concepts
│   ├── readings_extra7.py  ← readings for round-7 concepts
│   ├── readings_extra8.py  ← readings for round-8 concepts
│   ├── readings_extra9.py  ← readings for round-9 concepts
│   ├── readings_extra10.py ← readings for round-10 concepts
│   ├── readings_extra11.py ← readings for round-11 concepts
│   ├── merge.py            ← merges readings into ideas.json + validates full coverage
│   ├── atlas_graph.json    ← the constellation (425 nodes / 700 links) — a maintained data file
│   ├── ideas.json          ← GENERATED by the build (kept here for convenience)
│   ├── vendor/             ← pinned D3 v7 + base64-inlined fonts (self-hosted, no CDN)
│   └── template.html       ← the viewer; __IDEAS_JSON__/__ATLAS_JSON__/__D3_JS__/__FONTS_CSS__ injected at build
├── migrations/
│   ├── add_stars.py        ← one-time scripts that grew the constellation 376 → 425 stars.
│   └── add_stars2.py         Already applied to atlas_graph.json. Idempotent. Not part of build.
├── scripts/
│   └── vendor_assets.py    ← one-time vendoring of D3 + fonts into src/vendor/ (not part of build)
├── tests/
│   └── smoke.mjs           ← headless jsdom test of the built page (36 assertions)
├── build.py                ← the reproducible build
├── LICENSE                 ← CC BY 4.0
├── NOTICE                  ← third-party licenses (D3 ISC, fonts OFL 1.1)
├── package.json
└── README.md
```

---

## How it's built

Two data layers, one viewer.

1. **The idea layer** is authored in Python. `concepts.py` holds the base; `expand.py`,
   `expand2.py` … `expand11.py` layer on later rounds (each imports the previous so
   order is fixed). Running `expand11.py` applies all eleven rounds and writes `ideas.json`. Then
   `merge.py` attaches each concept's ranked thinkers and readings from the `readings*.py`
   files and **fails loudly if any concept lacks readings** or references an unknown id.

2. **The people layer** is `atlas_graph.json` — a star-chart of thinkers and schools grouped
   into West / Psyche / East. It started at 376 nodes and was grown to 425 by the scripts in
   `migrations/`. Those are already applied; the JSON is now the source of truth. (The scripts
   are idempotent if you ever re-run them, but you don't need to.)

3. **The viewer** (`template.html`) gets both JSON blobs spliced in at build time. It's
   vanilla JS + D3 v7, no framework. Cross-linking between layers is by normalized name match
   (with a small alias table and an unambiguous-surname fallback).

### The five modes

- **Questions** — the eleven great questions, with rival answers side by side.
- **Timeline** — every idea at its era, in West/Psyche/East/Indigenous lanes; dot size = how
  many later ideas build on it; hover traces its kin as arcs.
- **Idea Web** — the force-directed graph of concepts, coloured by question.
- **Threads** — the 18 dialectics walked one move at a time (thesis → antithesis → synthesis).
- **Constellation** — the original star-chart of thinkers and schools.

Cross-cutting: a filter rail (question / region / era / reading status), a per-idea
**reading tracker** (unread / reading / read, persisted in `localStorage`), an
**"argues against"** foil on every dossier, and **deep-linkable URLs**
(`#/timeline/idea/The-Burnout-Society`, `#/threads/2.1`).

---

## Editing the content

- **Add or change an idea:** edit the relevant `expandN.py` (or `concepts.py`) — append a
  tuple `(id, question, tradition, era, gloss, [thinkers])` to its `NEW_C*` list, add any
  links to `NEW_R*`, then add a readings entry in the matching `readings_extra*.py`. Rebuild.
  `merge.py` will refuse to build if the idea has no readings.
- **Add a thinker to the constellation:** add a node + links to `src/atlas_graph.json`
  (see the migration scripts for the shape). Names must match the concept-layer spelling for
  the cross-links to light up.
- Always `npm test` after editing.

---

## Deploying

It's a single static file at the repo root, so:

```bash
# GitHub Pages: commit index.html, then Settings → Pages → Source = "Deploy from a branch",
#   branch = main, folder = / (root). The prebuilt index.html is served as-is (no CI build).
# Netlify / Cloudflare Pages / Vercel: publish dir = repo root, no build command needed.
```

No environment variables, no server, no database.

---

## Public-launch readiness

Originally built as a private research scaffold, then taken through a launch-readiness pass.

**Done:**

- [x] **Facts verified.** The original 167 concepts, 276 thinker attributions, and 292 readings
      were externally checked (dates, titles, authorship). One contested dating was softened; the
      rest held up. The round-four literary additions (44 concepts) are newly added and have
      **not yet been through the same external verification pass**. Errors may remain —
      corrections welcome via GitHub Issues.
- [x] **"How to read this" panel.** States the viewpoint (leans toward detachment), that the
      links are interpretive, and that several debates (consciousness theories, idealism vs
      materialism, free will) are *live*. Contested positions carry a "Contested" tag.
- [x] **Self-hosted assets.** D3 and both fonts are inlined into `index.html` — no Google
      Fonts (GDPR), no CDN single-point-of-failure. Zero runtime network calls.
- [x] **Deploy hygiene.** Title/description/OG tags + favicon in `<head>`; an empty-state
      message when no ideas match the filters; CC BY 4.0 LICENSE + NOTICE for bundled deps.
- [x] **Accessibility baseline.** Keyboard/ARIA/reduced-motion on the chrome; SVG nodes are
      focusable with labels; the "How to read this" dialog is a real focus trap.

**Still open (v2):**

- [ ] **Mobile.** The desktop layout is solid; a phone-first layout (outline instead of the
      force-graph hairball) is not built yet. The page notes it's best on desktop.
- [ ] **Screen-reader navigability of the graphs** (nodes announce, but relationships/arcs
      don't) and a **colour-blind audit** of the 11-hue question encoding.
- [ ] **Representation.** The round-four literary canon widened this — Japanese (Murasaki,
      Bashō), African and diaspora (Achebe, Césaire, the *Sundiata* epic), and Latin-American
      (García Márquez) voices entered through literature. Round five adds a first **African &
      Indigenous tier** under its own lane, on a strict bar — each concept carries a *named,
      modern author* and a *real, dated text* (Ramose & Gyekye on Ubuntu, Abímbólá on Àṣẹ,
      Viveiros de Castro on perspectivism, Deloria on sacred land, Mary Graham on Aboriginal
      law), with **no anonymous or outsider-ethnography source standing in as authority**. Round
      six adds a **Korean tier** (the Four-Seven Debate, Wŏnhyo's hwajaeng, Jinul, Donghak's
      *innaecheon*) in the East lane. Round seven adds the **scripture-born Abrahamic core**
      (Agape, Grace, Original Sin, Logos, Covenant, Imago Dei, Tikkun Olam, Tawhid, Fitra) — the
      scripture as the source, named theologians as the thinkers — and round eight fills the
      largest remaining **civilizational gaps**: Zoroastrian Persia, Nahua (Aztec) Mesoamerica,
      the *Epic of Gilgamesh*, Egyptian *ma'at*, and Heschel. Round nine adds a **Hermetic &
      Esoteric** tier (As Above So Below, Gnosis, the alchemical Magnum Opus, the Perennial
      Philosophy) plus Norse *Ragnarök* — the four esoteric truth-claims carry the **Contested**
      tag, presented as historically influential, not endorsed. Round eleven closes the
      **practice-text gap** — contemplative traditions whose unit is a *method*, not a thesis:
      hesychast prayer of the heart (Gregory Palamas), the Ignatian *Spiritual Exercises*,
      Dzogchen (Longchenpa), the Buddha's four foundations of mindfulness, and Sufi *dhikr* (Ibn
      ʿAṭāʾ Allāh), each on the same named-author / real-dated-text bar. Still open: deeper coverage and
      genuinely oral/shamanic cosmologies that the readings gate can't yet cite honestly; the map
      still leans toward the detachment thesis it was built around. The framing panel owns this;
      broadening the canon further is ongoing work.
- [ ] **Link-preview image** (`og:image`) for richer social-share cards.

---

## Tech notes

- Vanilla JS + D3 v7 and Cormorant Garamond + Inter, all **self-hosted / inlined** into the
  single `index.html`. No build step for the viewer itself beyond JSON + asset injection.
- `localStorage` key `aoi.reading.v1` holds the reading tracker. Per-browser, no sync.
- No analytics, no cookies set by the app, **no runtime network calls at all**.
- Data integrity is enforced at build time: no orphan concepts, full reading coverage, no
  isolated stars.
